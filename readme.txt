SoloQ:
	First in data_extraction you can use extract_soloq_games.py to extract all the data
		from soloq games.
	Second soloq_clustering.py is for creating the dataset clustered of games.
	Then bayes_predictions.py takes the data clustered, creates a bayes nets model
		that you can use to predict data.